name: Build Blackbox Vault APK
on:
  push:
    branches: [ blackbox-vault-apk ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Check vault JavaScript
        run: node --check app/src/main/assets/app.js
      - name: Build unsigned release APK
        run: gradle :app:assembleRelease --stacktrace
      - name: Verify unsigned APK
        run: |
          test -f app/build/outputs/apk/release/app-release-unsigned.apk
          unzip -t app/build/outputs/apk/release/app-release-unsigned.apk >/dev/null
      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release-unsigned.apk Blackbox-Vault-unsigned.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Blackbox-Vault-Unsigned
          path: Blackbox-Vault-unsigned.apk
          if-no-files-found: error
